// Re-export the canonical LearningProgress model.
// LearningProgress.js is the authoritative source used by all active controllers.
// This file exists as an alias so any code importing from either path works correctly.
export { default } from './LearningProgress.js';
